{
  "required_paths": [
    "meta.T",
    "meta.delta_t",
    "config.max_iter",
    "config.tol",
    "config.use_generator",
    "config.use_distribution_grid",
    "sets.time",
    "sets.groups",
    "sets.arcs",
    "sets.stations",
    "sets.ev_stations",
    "sets.hybrid_stations",
    "parameters.q",
    "parameters.VOT",
    "parameters.lambda",
    "parameters.arcs",
    "parameters.stations",
    "parameters.electricity_price",
    "parameters.vertiport_storage",
    "parameters.vertiport_cap_pax",
    "parameters.vt_departure_capacity_total",
    "parameters.vt_departure_capacity_fast",
    "parameters.vt_pax_per_departure_fast",
    "parameters.vt_pax_per_departure_slow",
    "parameters.vt_aircraft_init_by_station",
    "itineraries"
  ],
  "optional_fields": {
    "parameters.vt_departure_allowed": {
      "type": "dict[str,bool]",
      "default_behavior": "If omitted, every hybrid station is treated as VT-departure-allowed."
    },
    "parameters.vt_arrival_allowed": {
      "type": "dict[str,bool]",
      "default_behavior": "If omitted, every hybrid station is treated as VT-arrival-allowed."
    }
  },
  "notes": [
    "Optional VT permission fields are normalized in data_loader.py for backward compatibility.",
    "Arrival-only hybrid stations are represented by vt_departure_allowed=false and vt_arrival_allowed=true."
  ]
}
